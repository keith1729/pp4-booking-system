# from django.urls import path
# from .views import RegistrationView, LoginFormView
# from .forms import LoginForm
# from django.contrib.auth import views as auth_views

# # URL config
# urlpatterns = [
#     path('registration/', RegistrationView.as_view(template_name='users/registration.html'), name='users_registration'),
#     path('login/', LoginFormView.as_view(redirect_authenticated_user=True, template_name='users/login.html', authentication_form=LoginForm), name='login'),
#     path('logout/', auth_views.LogoutView.as_view(template_name='users/logout.html'), name='logout'),
# ]
